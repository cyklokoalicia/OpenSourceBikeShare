<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ siteName }}{% endblock %}</title>
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    {% block css %}{% endblock %}
</head>
<body class="d-flex flex-column min-vh-100">
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">
        <img src="{{ asset('images/logo_small.svg') }}" width="30" height="30" class="d-inline-block align-top" alt="Logo">
        {{ siteName }}
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            {% block menuItems %}
                <li class="nav-item">
                    <a class="nav-link" href="https://wiki.whitebikes.info/index.php/Ako_to_funguje%3F">{{ 'How it works'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="https://wiki.whitebikes.info/index.php/Ako_sa_zapoj%C3%ADm%3F">{{ 'Get Involved'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="https://wiki.whitebikes.info/index.php/Nie%C4%8Do_mi_nejde">{{ 'Help'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="https://wiki.whitebikes.info/index.php/Podrobn%C3%BD_manu%C3%A1l">{{ 'Manual'|trans }}</a>
                </li>
            {% endblock %}
        </ul>
        {% if app.user %}
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('logout') }}">{{ 'Logout'|trans }}</a>
            </li>
        </ul>
        {% endif %}
    </div>
</nav>

<div class="container flex-grow-1">
    {% block body %}{% endblock %}
</div>

<footer class="footer mt-auto">
    <div class="card">
        <div class="card-footer">
            <div>
                <i class="fas fa-copyright"></i> {{ "now"|date("Y") }} <a href="{{ path('home') }}">{{ siteName }}</a>
            </div>
            <div>
                <strong>{{ 'Privacy policy:'|trans }}</strong> {{ 'We will use your details for'|trans }} {{ siteName }} - {{ 'related activities only'|trans }}.
            </div>
        </div>
    </div>
</footer>

<script src="//code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="//cdn.jsdelivr.net/npm/popper.js@1.14.3/dist/umd/popper.min.js"></script>
<script src="//stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
{% if googleAnalyticsId is not empty %}
    {% include ('google.analytics.html.twig') %}
{% endif %}
{% block scripts %}{% endblock %}
</body>
</html>
